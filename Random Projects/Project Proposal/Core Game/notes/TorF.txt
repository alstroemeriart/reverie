Python is a high-level programming language. = True
A compiler translates code line-by-line during execution. = False
An interpreter translates code one line at a time. = True
Variables store data values in memory. = True
The print() function receives user input. = False
The input() function reads data from the user. = True
Strings must always use double quotes only. = False
The + operator can concatenate strings. = True
len() returns the number of characters in a string. = True
The # symbol creates comments in Python. = True
Comments are executed as part of the program. = False
Floor division returns only the integer quotient. = True
Modulo returns the remainder of a division. = True
PEP 8 is Python’s official style guide. = True
Implicit conversion requires manual casting by the programmer. = False
int() converts values into integers. = True
float() converts values into floating-point numbers. = True
str() converts values into strings. = True
A for loop is best when the number of iterations is known. = True
A while loop always runs at least once. = False
An if statement executes only when its condition is true. = True
elif allows checking multiple conditions. = True
Variables can start with numbers in Python. = False
Python ignores spaces at the beginning of lines. = False
A docstring is used for program documentation. = True
Multiplying a string by an integer repeats the string. = True
True division always returns a float result. = True
range() generates a sequence of integers. = True
Functions allow grouping statements for reuse. = True
Machine code is written in binary form. = True
Machine code is written in binary form. = True 
An algorithm is written in a specific programming language. = False 
A flowchart is a visual program design tool. = True 
The "Oval" symbol in a flowchart represents a process. = False 
Pseudocode is a set of instructions that mimic programming language. = True 
Python was developed by Guido van Rossum. = True 
High-level languages are languages that only computers can understand. = False 
A compiler translates the whole program into machine code at once. = True 
An interpreter translates code line-by-line at runtime. = True 
Variables are fundamental storage units that act as containers. = True 
Static variables are allocated at runtime and managed by programmers. = False 
Relational operators are used to compare two values. = True 
In Python, spaces at the start of a line (indentation) are very important. = True 
The if statement executes code only if the condition is false. = False 
PEP 8 is the official style guide for Python. = True 
The sep attribute can be used to customize the print() function output. = True 
A variable name can start with a digit. = False 
Single and double quotes can both be used to enclose strings. = True 
The len() function returns the memory address of a string. = False 
Floating-point format represents whole numbers. = False 
The # character is used for writing comments in Python. = True 
Python uses implicit type conversion to automatically change data types. = True 
The * operator can be used as a repetition operator for strings. = True 
Floor division (//) computes the remainder of a division. = False 
Lists must be created inside the loop to store qualifying elements. = False 
Tuples are immutable, meaning you cannot change their values after creation. = True 
The .items() method is required to loop through both dictionary keys and values. = True 
A while loop is used when the range of iterations is certain. = False 
In recursion, the base case is where the function stops calling itself. = True 
Sets in Python automatically handle deduplication. = True